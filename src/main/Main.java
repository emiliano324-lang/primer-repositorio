package main;

import controllers.HomeController;
import controllers.LoginController;
import controllers.RegistrationController;
import views.GameMenuView;
import views.GameWindow;
import views.LoginWindow;
import views.MainWindow;
import views.RegistrationWindow;
/**
 * Clase de entrada principal de la aplicación.
 * Se encarga de iniciar el ciclo de vida del software abriendo la interfaz de inicio de sesion.
 * @author Hugo 
 * @author Emiliano 
 * @version 1.0
 */
public class  Main {
	
	/**
	 * Metodo de arranque del sistema.
	 * Inicializa la ventana de login para que el usuario pueda autenticarse e interactuar con el juego.
	 * 
	 */
	public static void main(String[] args) {
		
		LoginWindow login = new LoginWindow();
		

		

	}
	
}
